node_modules/
dist/
.DS_Store
*.log

# Downloaded source assets, before processing. Regenerate with npm run assets:fetch.
.assetcache/

# Critic harness output.
captures/

# Frozen tree served by the capture harness, one per concurrent run
.capture-snapshot/
.capture-snapshot-*/
